[pytest]
# Put the repo root on sys.path so `import lib.plan_parser` works without installing.
pythonpath = .
testpaths = tests
addopts = -q
markers =
    integration: tests that need a real `bd` binary + temp Dolt repo (skipped if bd absent)
