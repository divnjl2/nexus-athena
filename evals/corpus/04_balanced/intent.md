# Intent

Implement `is_balanced(s: str) -> bool` in `balanced.py`.

It returns True iff every bracket in `s` is correctly matched and nested, considering the
three bracket pairs `()`, `[]`, and `{}`. Non-bracket characters are ignored.

This is the only input to the planner. The eval measures how complete a plan the real
Athena pipeline derives from this one-line intent.
